spring:
  h2:
    console:
      enabled: true
      path: /console
  datasource:
    url: jdbc:h2:~/test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-plattform: org.hibernate.dialect.H2Dialect
    generate-ddl: true

